<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>
    <link rel="icon" type="image/png" href="/IMAGES/notebook.png">
    <link rel="stylesheet" href="/CSS/page_one.css">
</head>
<body>
    <div class="container">
        <h1>Edit Item</h1>
        
        <form action="/itemList/<%= item._id %>?_method=PUT" method="POST">
            <label for="name">Item Name:</label>
            <textarea name="name" id="name" required><%= item.name %></textarea>

            <div class="checkbox-container">
                <input type="checkbox" name="completed" id="completed" <%= item.completed ? 'checked' : '' %>>
                <label for="completed">Completed</label>
            </div>

            <button type="submit">Save Changes</button>
        </form>

        <a href="/itemList">Cancel</a>
    </div>
</body>
</html>
